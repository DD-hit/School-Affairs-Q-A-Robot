#  校务问答机器人系统（原型）— 需求变更请求50条

> **适用范围**：本批变更仅针对前端原型（HTML/CSS/JS + mock数据），不涉及真实后端、真实AI接入、生产部署等。所有变更均可在原型层面模拟实现并演示。
>
> **依据文档**：网页原型v2.0（7页面）、QFD需求矩阵（UR-001~034，AD-001~022，NFR-001~028）、协作图/对象图、用例文档、设计系统文档、FINAL_CHECKLIST、教师访谈记录、30条甘特图执行方案。

---

## 一、功能新增类（16条）

### CR-001 新增"预约人工客服"模拟功能 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 骆彦余辅导员访谈："机器人解决不了的问题要有渠道转到真人" |
| **变更描述** | 当问答机器人连续两次回答被用户评价为"没帮助"后，自动弹出一个"预约人工咨询"入口卡片。用户点击后进入预约表单（模拟），选择咨询方式（电话回拨/线下窗口/企业微信），选择时间段，提交后显示"已提交，工作人员将在1个工作日内联系您"的模拟成功提示。个人中心新增"我的预约"记录列表。 |
| **变更理由** | 兜底机制是问答系统的必备环节；当前原型仅展示"无法回答的指引"，缺少主动转人工的流程。 |
| **受影响文件** | `index.html`（预约入口卡片）、`personal.html`（预约记录列表）、`script.js`（预约逻辑+mock预约数据） |
| **优先级** | 高 |
| **工作量** | 中（约需1天） |

---

### CR-002 新增"常见文件下载"专区 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 第一次访谈中苏奎老师反馈："学生经常要找各种表格和模板" |
| **变更描述** | 在问答首页增加"常用下载"模块，列出10类常用文件模板（奖学金申请表、请假条模板、宿舍调换申请表、校园卡挂失表、在读证明模板等）。每个文件提供"下载"按钮（模拟下载，实际上是跳转到一个提示页"文件下载功能将在正式版中提供"）。支持按分类筛选和文件名搜索。 |
| **变更理由** | 文件下载是高频校务需求；目前原型缺少资源下载类功能；通过mock实现可演示完整交互流程。 |
| **受影响文件** | `index.html`（常用下载区域）、`script.js`（下载分类+mock数据）、`styles.css` |
| **优先级** | 中 |
| **工作量** | 小（约需半天） |

---

### CR-003 新增个人中心"我的收藏"功能 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | QFD需求矩阵UR-009（点赞/收藏回答内容）——当前实现了点赞，未实现收藏的独立管理 |
| **变更描述** | 在答案展示区增加"收藏"按钮（五角星图标）。用户收藏的答案集中展示在个人中心"我的收藏"tab页，按收藏时间倒序排列。支持取消收藏和批量清空。收藏数据通过localStorage持久化（关闭浏览器后仍保留）。 |
| **变更理由** | UR-009"点赞/收藏"是两项不同需求，当前原型只做到点赞；收藏是用户高频使用的信息管理功能。 |
| **受影响文件** | `index.html`（收藏按钮）、`personal.html`（收藏列表tab）、`script.js`（收藏逻辑+localStorage持久化） |
| **优先级** | 高 |
| **工作量** | 小（约需半天） |

---

### CR-004 新增论坛帖子分页/无限滚动 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 测试与文档专员（聂东阳）在测试用例中标注："论坛帖子仅展示3条mock数据，无法验证分页场景" |
| **变更描述** | 扩充论坛mock数据至30条帖子（覆盖全部6个分类），实现"滚动到底部自动加载更多"（无限滚动），每次加载6条。加载时底部显示"加载中..."动画。同时提供"回到顶部"浮动按钮。 |
| **变更理由** | 当前3条帖子无法验证论坛浏览体验；无限滚动是论坛类产品的常见交互模式；扩充数据后可验证筛选+搜索+分页的组合场景。 |
| **受影响文件** | `forum.html`（滚动区域）、`script.js`（mock数据扩充+无限滚动逻辑）、`styles.css`（加载动画+回到顶部按钮） |
| **优先级** | 中 |
| **工作量** | 中（约需1天） |

---

### CR-005 新增管理员后台"数据看板"可视化图表 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | QFD需求矩阵AD-002~005（统计需求），当前admin.html仅有静态数字展示 |
| **变更描述** | 将管理员后台首页的统计数据升级为可视化看板：①折线图展示近7天问答量趋势；②饼图展示各分类问题占比；③柱状图展示每日访问量；④热门问题TOP10横向条形图。使用纯JS绘制（Canvas或SVG），不引入第三方图表库。数据为mock随机生成，每次刷新略有变化以模拟真实感。 |
| **变更理由** | 当前统计数据仅为4个静态数字；可视化图表是数据看板的标准形态；纯JS实现不增加外部依赖。 |
| **受影响文件** | `admin.html`（图表区域DOM）、`script.js`（图表绘制函数+mock趋势数据生成） |
| **优先级** | 中 |
| **工作量** | 中（约需1天） |

---

### CR-006 新增问题输入时的联想补全 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 用例与原型设计师（罗南希）在原型评审中提到："搜索框缺少输入提示，用户不知道该问什么" |
| **变更描述** | 在问答搜索框输入时，根据已输入的文字实时弹出联想下拉列表（最多5条建议），建议来源为热门问题库和知识库标题。用户可用键盘↑↓选择、Enter或鼠标点击确认。联想匹配规则：从开头匹配 → 包含匹配，优先展示以输入文字开头的问题。 |
| **变更理由** | 引导用户提问，降低"不知道问什么"的障碍；联想补全是搜索框的标配交互。 |
| **受影响文件** | `index.html`（联想下拉框）、`script.js`（联想匹配+mock问题库索引）、`styles.css`（下拉框样式） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-007 新增"夜间模式自动切换"定时功能 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 设计系统文档中深色模式色彩定义齐全，但切换仅支持手动 |
| **变更描述** | 在现有手动切换基础上，增加自动切换选项：用户可设置"每天18:00自动切换深色 / 06:00自动切回浅色"。个人中心的主题设置处增加"自动切换"开关和时间选择器。若用户同时设置了自动切换和手动切换，以最近一次操作为准。 |
| **变更理由** | 手动切换需要用户每次操作；定时自动切换更符合实际使用习惯；实现简单但用户体验提升明显。 |
| **受影响文件** | `personal.html`（自动切换设置UI）、`script.js`（定时器逻辑+localStorage存储偏好时间） |
| **优先级** | 低 |
| **工作量** | 小（约需半天） |

---

### CR-008 新增"通知中心"页面 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | AD-019（发布/编辑系统公告）——发布了公告但用户端无统一查看入口 |
| **变更描述** | 新建`notifications.html`页面，作为全校通知的统一查看入口。页面包含：①通知列表（按时间倒序，标记已读/未读状态）；②分类筛选（系统通知/校务公告/回复提醒/活动推送）；③点击通知进入详情；④"全部标记已读"按钮；⑤导航栏增加通知铃铛图标+未读红点数字。mock数据包含至少15条各类通知。 |
| **变更理由** | 当前原型缺少通知聚合页；通知中心是连接管理员公告与普通用户的桥梁；未读红点是提升用户回访率的经典设计。 |
| **受影响文件** | 新建`notifications.html`、全部页面的导航栏（增加铃铛图标）、`script.js`（通知mock数据+未读计数）、`styles.css` |
| **优先级** | 高 |
| **工作量** | 大（约需1-2天） |

---

### CR-009 新增回答内容"一键复制/分享"功能 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 需求获取阶段用户反馈："想把机器人回答的内容分享给同学" |
| **变更描述** | 每条机器人回答下方增加两个小按钮：①"复制"——点击后复制回答全文到剪贴板，按钮短暂变为"已复制✓"（1.5秒后恢复）；②"分享"——点击后弹出分享面板，模拟分享到微信/QQ/企业微信（实际仅展示提示"链接已复制，请粘贴分享"）。 |
| **变更理由** | 信息分享是高频需求；复制和分享是内容型产品的标配操作；纯前端实现简单（Clipboard API + mock分享面板）。 |
| **受影响文件** | `index.html`（回答区按钮）、`script.js`（剪贴板API+分享面板逻辑）、`styles.css`（分享面板样式） |
| **优先级** | 中 |
| **工作量** | 小（约需2-3小时） |

---

### CR-010 新增登录页"忘记密码"流程模拟 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 测试用例文档中缺失"忘记密码"的异常流程测试场景 |
| **变更描述** | 在登录页增加"忘记密码？"链接，点击后进入找回密码流程（三步）：①输入注册邮箱/手机号 → ②"验证码已发送至您的手机"模拟提示（倒计时60秒） → ③输入验证码+新密码 → 提示"密码重置成功，请重新登录"。整个过程为前端模拟，验证码固定为"123456"。 |
| **变更理由** | UR-001（用户注册）和UR-002（用户登录）缺少密码找回的异常流程；登录模块的完整性需要覆盖"忘记密码"路径。 |
| **受影响文件** | `login.html`（新增找回密码流程DOM）、`script.js`（找回密码状态机）、`styles.css` |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-011 新增问答对话的"快捷回复"短语 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | QFD中UR-015（无法回答兜底指引）仅覆盖了"机器人答不上来"的场景，未覆盖"用户想追问"的场景 |
| **变更描述** | 在机器人每条回答下方增加3个快捷追问按钮，根据回答内容动态生成。例如回答"奖学金申请流程"后，下方显示：「📌 需要什么材料？」「📅 截止日期是？」「📞 咨询电话是？」。用户点击后自动以该问题发起追问。快捷追问内容在mock答案数据中预定义。 |
| **变更理由** | 模拟真实对话中的追问场景；降低用户二次输入成本；让问答原型展示更完整的对话交互链路。 |
| **受影响文件** | `index.html`（快捷回复按钮）、`script.js`（mock数据增加followUp字段+快捷回复逻辑） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-012 新增"校园黄页"页面 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 第一次访谈中王豪烨老师反馈："学生经常找不到各部门电话，要有个统一查询的地方" |
| **变更描述** | 新建`directory.html`页面，展示校内各部门联系方式。包含：①按部门类别筛选（教学/学工/后勤/财务/信息中心/保卫处等）；②每个部门卡片含名称、办公地点、电话、办公时间、主要职责简介；③顶部搜索框支持按部门名称或职责关键词模糊搜索。mock数据覆盖至少20个校内部门。 |
| **变更理由** | 部门联系方式查询是校务问答的基础需求；当前原型仅在"无法回答"时展示零星联系方式，缺少系统性的信息查询入口。 |
| **受影响文件** | 新建`directory.html`、导航栏增加"校园黄页"入口、`script.js`（部门数据+搜索）、`styles.css` |
| **优先级** | 中 |
| **工作量** | 中（约需1天） |

---

### CR-013 新增帖子详情页"相关帖子推荐" T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 协作图中post.html仅处理单帖查看，缺少"关联推荐"交互 |
| **变更描述** | 在帖子详情页底部增加"相关推荐"区域，显示2-3篇同分类或标题关键词相似的帖子。推荐逻辑（模拟）：优先同分类→其次标题关键词匹配→随机补足。点击推荐卡片直接切换到该帖子详情。 |
| **变更理由** | 增加用户在论坛中的浏览深度；同分类推荐是论坛类产品的常见功能；模拟实现简单。 |
| **受影响文件** | `post.html`（推荐区域）、`script.js`（推荐算法mock逻辑） |
| **优先级** | 低 |
| **工作量** | 小（约需2-3小时） |

---

### CR-014 新增"操作引导浮层"（首次使用提示）F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 测试与文档专员（聂东阳）用户手册编写中发现："新用户可能不知道怎么用这个系统" |
| **变更描述** | 首次访问（localStorage无"visited"标记）时，在问答首页展示一个3步引导浮层（高亮+文字说明）：步骤1：指向搜索框"在这里输入您的校务问题"；步骤2：指向分类区"或按分类快速查找"；步骤3：指向热门问题"看看大家都在问什么"。点击"知道了"或×关闭后不再显示。可随时从帮助页重新触发引导。 |
| **变更理由** | 降低新用户的认知门槛；引导浮层是提升功能发现率的标准做法；localStorage记录首次访问状态。 |
| **受影响文件** | `index.html`（引导浮层DOM）、`script.js`（引导步骤状态机+localStorage标记）、`styles.css`（引导浮层样式） |
| **优先级** | 低 |
| **工作量** | 中（约需半天） |

---

### CR-015 新增问题分类"标签云"展示模式 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | 当前分类网格占空间较大，用户（罗南希原型评审）建议增加紧凑模式 |
| **变更描述** | 在分类网格视图旁增加"标签云"切换按钮。标签云模式下，分类以不同大小和颜色的标签形式展示（字号大小对应问题数量多少）。用户可在网格视图和标签云之间切换，偏好通过localStorage记忆。 |
| **变更理由** | 提供多种视觉呈现方式，适应不同用户偏好；标签云可在一屏内展示更多分类信息。 |
| **受影响文件** | `index.html`（标签云DOM+切换按钮）、`script.js`（标签云渲染+视图切换）、`styles.css`（标签云样式） |
| **优先级** | 低 |
| **工作量** | 小（约需2-3小时） |

---

### CR-016 新增"聊天对话导出为图片"功能 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟢 新增 |
| **变更来源** | UR-014（导出历史对话记录）——当前仅有mock入口，未实现任何导出 |
| **变更描述** | 在对话区和个人中心历史对话详情中增加"导出为图片"按钮。点击后将当前对话内容渲染到一个隐藏的canvas中，生成长图（含问答气泡、时间戳），触发浏览器下载（PNG格式）。导出范围：当前对话或选中的历史对话。图片顶部自动添加"校务问答机器人"水印和导出时间。 |
| **变更理由** | UR-014标注为需求但原型中完全未实现；导出图片比导出文本更直观（可直接分享到社交平台）；使用html2canvas或纯DOM截图思路实现。 |
| **受影响文件** | `index.html`（导出按钮）、`personal.html`（历史对话导出按钮）、`script.js`（导出逻辑） |
| **优先级** | 低 |
| **工作量** | 中（约需1天） |

---

## 二、功能修改类（16条）

### CR-017 问答首页布局从"英雄区+搜索"改为"搜索居中+卡片式" F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 设计系统文档提倡"简洁清爽"，但当前index.html顶部英雄区（蓝色渐变banner）占屏过大 |
| **变更描述** | 将首页布局从"占屏1/3的蓝色渐变大banner + 搜索框"改为更紧凑的"顶部导航栏下方直接居中展示搜索框 + 下方卡片式功能入口（问答/论坛/黄页/下载四宫格卡片）"。移除hero-section的蓝色渐变背景，将搜索框与分类无缝衔接。热门问题标签移到搜索框下方折叠区域（默认展示5个，点击"更多热门问题"展开全部）。 |
| **变更理由** | 当前英雄区占用首屏约400px高度，用户需滚动才能看到核心功能；更紧凑的布局符合"简洁清爽、无冗余元素"的设计原则。 |
| **受影响文件** | `index.html`（DOM结构重构）、`styles.css`（首页样式大幅修改） |
| **优先级** | 高 |
| **工作量** | 大（约需1天） |

---

### CR-018 论坛分类从6类扩展为8类 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 第一次访谈中多位老师提及遗漏了"就业实习""国际交流"场景 |
| **变更描述** | 论坛分类从现有6类（全部/选课/奖助勤贷/宿舍/校园卡/其他）扩展为8类：全部、选课与成绩、奖助勤贷、宿舍生活、校园卡与消费、图书馆、就业与实习、国际交流。分类筛选栏从2行改为2行（每行4个tab）。 |
| **变更理由** | 当前分类遗漏了"就业实习"和"国际交流"两个重要场景；与CR-015（12类问答分类）保持部分对齐但论坛分类更精简。 |
| **受影响文件** | `forum.html`（分类tab）、`script.js`（forumCategories数组+帖子mock数据分类字段更新） |
| **优先级** | 中 |
| **工作量** | 小（约需1-2小时） |

---

### CR-019 管理员后台知识库管理增加搜索和筛选 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | AD-006（查询知识库条目）——当前列表无搜索和筛选功能 |
| **变更描述** | 在管理员知识库管理面板顶部增加搜索框（按问题/答案关键词搜索）和分类下拉筛选器。搜索为即时过滤（输入即时过滤列表，不需要点搜索按钮），支持同时按分类+关键词组合筛选。搜索结果高亮匹配的关键词。 |
| **变更理由** | AD-006"查询知识库条目"要求可查询，当前仅能浏览列表；知识库条目增加后（如30+条），无搜索功能无法管理。 |
| **受影响文件** | `admin.html`（搜索框+筛选器DOM）、`script.js`（即时过滤逻辑+关键词高亮） |
| **优先级** | 高 |
| **工作量** | 小（约需2-3小时） |

---

### CR-020 个人中心从单栏布局改为Tab切换布局 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 当前personal.html使用长滚动单页布局，内容混杂（用户信息→历史记录→设置→反馈→帖子挤在一起） |
| **变更描述** | 将个人中心重构为Tab切换布局，顶部展示用户头像+基本信息卡片（固定不动），下方为Tab栏：「聊天记录」「我的收藏」「我的帖子」「我的反馈」「设置」。点击Tab切换下方内容区，避免长滚动。当前激活的Tab通过URL hash记录（如personal.html#history），支持浏览器前进/后退。 |
| **变更理由** | 单栏布局信息密度过大、导航效率低；Tab切换可在一屏内展示更多内容类别；URL hash支持可收藏特定Tab。 |
| **受影响文件** | `personal.html`（布局重构）、`script.js`（Tab切换+hash路由）、`styles.css`（Tab布局样式） |
| **优先级** | 高 |
| **工作量** | 大（约需1天） |

---

### CR-021 管理员后台侧边栏增加子菜单展开/折叠 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | admin.html当前侧边栏6个菜单项平铺，多个管理功能视觉上等权重 |
| **变更描述** | 将侧边栏菜单改为二级结构：①数据概览（含今日统计、趋势图表）；②内容管理（含知识库管理、论坛管理、公告管理）；③用户管理（含用户列表、反馈处理）；④系统设置（含参数配置、操作日志）。一级菜单支持展开/折叠（手风琴效果），当前激活的子菜单高亮。默认展开"数据概览"。 |
| **变更理由** | 平铺菜单在功能增多后不够用；二级结构更符合管理后台的信息架构惯例。 |
| **受影响文件** | `admin.html`（侧边栏DOM改造）、`script.js`（菜单展开折叠逻辑）、`styles.css`（子菜单样式+折叠动画） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-022 帖子发布从独立post.html改为论坛页内弹窗 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 协作图显示"发帖需跳转到post.html"，增加了一次页面跳转 |
| **变更描述** | 在forum.html中增加一个"发帖"模态弹窗（而非跳转到post.html）。用户在论坛页点击"发帖"按钮后，弹出居中的发帖表单（标题+分类选择+正文+提交按钮），填写完成后提交，帖子即时追加到列表顶部。post.html保留用于查看帖子详情+评论区。 |
| **变更理由** | 发帖不需要独立页面（表单简单）；模态弹窗减少页面跳转，保持用户浏览上下文；post.html专注做"查看详情+评论"。 |
| **受影响文件** | `forum.html`（发帖模态弹窗）、`post.html`（移除发帖模式，仅保留查看模式）、`script.js`（发帖逻辑迁移） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-023 热门问题标签从固定30条改为"本周热榜+历史热榜"双Tab T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | UR-029（查看热门话题榜单）未区分时间维度 |
| **变更描述** | 将热门问题区域从单列表改为"本周""本月"两个Tab。本周展示近7天点击量mock数据（6-10条，带"🔥"标记和点击数），本月展示近30天数据。切换Tab时标签区有淡入淡出过渡动画。两个Tab的数据在mock中独立维护。 |
| **变更理由** | 区分时间维度让"热门"更有意义（选课周和考试周的热门完全不同）；Tab切换的过渡动画提升体验。 |
| **受影响文件** | `index.html`（热门问题区Tab）、`script.js`（双时间维度mock数据+切换动画） |
| **优先级** | 低 |
| **工作量** | 小（约需2-3小时） |

---

### CR-024 校务推送从"校级/院级"二元改为多级筛选 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | UR-030（查看校务消息推送）——当前仅校级和院级两个tab |
| **变更描述** | 将校务推送的标签从2个扩展为4个：全部 / 校级 / 院级 / 班级。增加"全部"作为默认视图（合并展示所有级别的通知，每条标注级别标签）。每个Tab的未读数量以小红点数字标注。Mock数据从6条扩充至12条（每级3条）。 |
| **变更理由** | 班级级别通知（辅导员通知、班级活动）是学生最关注的信息之一；"全部"Tab方便用户一次浏览所有通知。 |
| **受影响文件** | `index.html`（推送Tab组）、`script.js`（推送mock数据扩充+级别标签） |
| **优先级** | 低 |
| **工作量** | 小（约需1-2小时） |

---

### CR-025 登录/注册表单验证规则细化 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 测试用例文档中登录模块测试点覆盖不足 |
| **变更描述** | 细化登录和注册表单的前端验证规则：①用户名至少4位，仅允许字母/数字/下划线；②密码至少8位，必须包含字母和数字；③确认密码需与密码一致（实时校验，不一致时输入框红框提示）；④学号/工号按格式校验（如8位数字）；⑤手机号按11位数字校验。每个输入框失焦时触发校验，错误提示显示在输入框下方。所有校验通过后提交按钮才可点击（从disabled变为active）。 |
| **变更理由** | 当前表单验证较简单（仅判空）；更严格的验证规则可在原型中展示完整的"注册-校验-报错-修正"交互闭环。 |
| **受影响文件** | `login.html`（表单验证DOM+错误提示区域）、`script.js`（新增validateField函数+各类校验规则） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-026 答案展示区增加"此答案更新时间"标注 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 苏奎老师访谈："政策每年都在变，回答里看不出是哪年的信息" |
| **变更描述** | 在每条结构化答案的末尾增加一行灰色小字："📅 此答案更新于 2026年X月X日"。更新时间在mock数据中预设为不同日期（部分标记为"2025年"，以验证过期提示效果）。对于更新时间超过6个月的答案，增加一个淡黄色提示条："⚠️ 此答案已超过6个月未更新，信息可能已有变化，建议向相关部门确认。" |
| **变更理由** | 时效性是校务信息可信度的关键；通过mock数据中预设不同更新日期，可直观演示"保鲜期"机制。 |
| **受影响文件** | `index.html`（答案区更新时间DOM+过期提示）、`script.js`（mock数据增加updateTime字段+过期判断） |
| **优先级** | 高 |
| **工作量** | 小（约需2-3小时） |

---

### CR-027 论坛帖子卡片增加缩略摘要 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 当前帖子卡片在forum.html中仅显示标题+作者+时间，缺少内容预览 |
| **变更描述** | 每个帖子卡片增加一行内容摘要（截取正文前50字+省略号"……"）。如帖子有图片附件，摘要末尾显示"📷[图片]"。摘要颜色为辅助色（#8C8C8C），字号12px。 |
| **变更理由** | 标题+摘要帮助用户判断帖子是否值得点击；减少无效点击；符合论坛类产品信息展示惯例。 |
| **受影响文件** | `forum.html`（帖子卡片DOM）、`script.js`（摘要截取逻辑）、`styles.css`（摘要样式） |
| **优先级** | 低 |
| **工作量** | 小（约需1小时） |

---

### CR-028 管理员用户列表增加状态筛选和操作按钮 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | AD-010~012（用户管理），当前用户列表仅有静态展示 |
| **变更描述** | 用户列表增加：①状态筛选（全部/正常/已封禁）；②每行用户增加"查看详情""封禁/解封""删除"三个操作按钮；③封禁操作弹出确认框+封禁原因填写（模拟）；④列表支持按用户名搜索。mock用户数据从5条扩充至20条，状态随机分配（15正常、3已封禁、2未激活）。 |
| **变更理由** | AD-011要求"封禁/解封用户账号"，当前原型无法演示此操作；增加交互按钮让管理员后台原型更具演示价值。 |
| **受影响文件** | `admin.html`（用户列表+操作DOM）、`script.js`（用户mock数据扩充+操作逻辑） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-029 导航栏"个人中心"入口改为用户头像+下拉菜单 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | 设计系统文档中导航栏规范未涉及用户头像展示 |
| **变更描述** | 将导航栏右侧的"个人中心"文字链接改为"用户头像圆圈（显示姓氏首字）+ 用户名 + 下拉箭头"。点击弹出下拉菜单：个人中心 / 我的通知 / 我的收藏 / 设置 / 退出登录。未登录时显示"登录"按钮。 |
| **变更理由** | 头像+下拉菜单是Web应用的标配导航模式；释放导航栏空间（原来需要"个人中心""管理员后台""使用帮助"三个独立链接）；下拉菜单可容纳更多入口。 |
| **受影响文件** | 全部7个HTML页面（导航栏改造）、`script.js`（下拉菜单交互）、`styles.css`（头像+下拉菜单样式） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-030 使用帮助页增加FAQ折叠面板 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | UR-033（查看帮助文档）——当前help.html为静态文字列表 |
| **变更描述** | 将帮助页内容从静态文字列表改为折叠面板（Accordion）。每个FAQ问题作为面板标题，点击展开显示答案。支持"全部展开/全部折叠"切换按钮。面板展开/折叠有平滑过渡动画（max-height过渡）。FAQ数据在mock中维护（至少15条），包含使用类、功能类、账号类三类。 |
| **变更理由** | 折叠面板可在一页内展示更多FAQ条目且不造成信息过载；展开/折叠动画提升体验。 |
| **受影响文件** | `help.html`（折叠面板DOM）、`script.js`（折叠面板逻辑）、`styles.css`（面板动画样式） |
| **优先级** | 低 |
| **工作量** | 小（约需2-3小时） |

---

### CR-031 回答"点赞"图标从对勾改为大拇指+数字 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | UR-009（点赞/收藏回答内容）——当前点赞图标为Font Awesome的fa-check（对勾），语义不够直观 |
| **变更描述** | 将答案下方的点赞按钮图标从对勾（fa-check）改为大拇指（fa-thumbs-up）+ 点赞数字显示。点击后大拇指变实心+颜色变为主色蓝+数字+1，再次点击取消点赞。点赞数在mock数据中预设不同值（0/3/15/89等），非全部从0开始，以模拟真实场景。 |
| **变更理由** | 大拇指是"点赞"的通用视觉语言，对勾可能被误解为"正确/确认"；数字显示点赞数有社交证明效果。 |
| **受影响文件** | `index.html`（点赞按钮图标）、`script.js`（点赞toggle逻辑+数字更新）、`styles.css`（点赞激活态样式） |
| **优先级** | 低 |
| **工作量** | 小（约需1-2小时） |

---

### CR-032 管理员后台"反馈处理"增加批量操作 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔵 修改 |
| **变更来源** | AD-016~018（反馈管理），当前为逐条操作 |
| **变更描述** | 反馈管理列表增加：①每行左侧增加复选框；②顶部增加"全选""批量标记已处理""批量删除"操作栏；③选中反馈后操作栏显示选中数量；④批量操作前弹出二次确认框。mock反馈数据从3条扩充至15条，状态混合（未处理/处理中/已处理/已关闭）。 |
| **变更理由** | 反馈量增加后逐条操作效率低；批量操作是后台管理的标配；复选框+批量操作的交互模式可在原型中完整体现。 |
| **受影响文件** | `admin.html`（反馈列表+批量操作栏）、`script.js`（批量选择+批量操作逻辑） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

## 三、功能删除/降级类（5条）

### CR-033 删除个人中心的"多模型切换"mock功能 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔴 删除 |
| **变更来源** | UR-019——当前在personal.html中有模型切换UI（GPT-4/文心一言/通义千问选项），但完全是mock占位，点击无任何实际效果 |
| **变更描述** | 从个人中心移除AI模型切换区域。该功能的mock实现在原型演示中容易引起评审老师对"为什么模型切换没效果"的追问。替换为"消息通知设置"区域（参见CR-051联动变更）。 |
| **变更理由** | 纯占位mock功能在演示中容易成为扣分点；删除后个人中心布局更紧凑。 |
| **受影响文件** | `personal.html`（移除模型切换区域）、`script.js`（移除相关代码） |
| **优先级** | 高 |
| **工作量** | 小（约需0.5小时） |

---

### CR-034 删除个人中心的"自定义知识库导入"mock功能 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔴 删除 |
| **变更来源** | personal.html中"知识库导入"区域——与CR-033同为占位mock |
| **变更描述** | 从个人中心移除知识库导入功能入口。演示时容易被质疑"普通用户导入的知识怎么保证质量"，且当前完全无实现。替换为"快捷入口"区域（常用链接：教务系统、图书馆、一网通办等）。 |
| **变更理由** | 与CR-033同理，删除无交互的占位mock；替换为更有实际展示价值的快捷入口。 |
| **受影响文件** | `personal.html`（替换区域内容） |
| **优先级** | 高 |
| **工作量** | 小（约需0.5小时） |

---

### CR-035 移除论坛帖子卡片的"浏览次数"动画 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔴 删除 |
| **变更来源** | 设计系统文档强调"简洁清爽、无花哨动画" |
| **变更描述** | 论坛帖子卡片上的浏览次数目前有一个数字跳动动画（如果存在），移除该动画，改为静态数字显示。如有其他纯装饰性CSS动画（非功能性的hover效果除外）一并移除。 |
| **变更理由** | 与设计系统文档自定的"无花哨动画"原则保持一致；减少视觉噪音。 |
| **受影响文件** | `forum.html`或`styles.css`（移除装饰性动画CSS） |
| **优先级** | 低 |
| **工作量** | 小（约需0.5小时） |

---

### CR-036 删除热门问题的"滚动条自定义样式" F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔴 删除 |
| **变更来源** | index.html中热门问题区域使用了`::-webkit-scrollbar`自定义滚动条，但在Firefox中完全不生效 |
| **变更描述** | 移除热门问题列表区域的自定义滚动条CSS规则，改用浏览器系统默认滚动条。 |
| **变更理由** | 自定义滚动条仅WebKit内核支持；在原型评审中如用Firefox打开会看到样式不一致；系统默认滚动条在可访问性方面更优。 |
| **受影响文件** | `index.html`（移除内联style中的::-webkit-scrollbar规则）、`styles.css`（如有） |
| **优先级** | 低 |
| **工作量** | 小（约需0.5小时） |

---

### CR-037 帖子详情页双模式简化为单模式（仅查看） F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🔴 删除 |
| **变更来源** | 项目完成总结中描述post.html为"双模式设计（查看模式和发帖模式一键切换）"——但发帖模式使用率低且增加代码复杂度 |
| **变更描述** | post.html简化为单一的"帖子详情+评论"查看模式。移除"发帖模式"切换按钮和相关代码。发帖功能移至forum.html的模态弹窗（参见CR-022）。post.html专注做好：帖子正文展示、评论区浏览、发表评论、点赞帖子。 |
| **变更理由** | 与CR-022联动，发帖功能迁移至论坛页弹窗，post.html职责更单一；减少post.html约30%的DOM和JS代码。 |
| **受影响文件** | `post.html`（移除发帖模式代码）、`script.js`（移除发帖模式切换逻辑） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

## 四、非功能需求变更类（8条）

### CR-038 全部页面增加loading骨架屏 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | NFR-006（所有操作无网络请求停顿感）——当前页面切换后有短暂白屏 |
| **变更描述** | 为问答首页、论坛页、个人中心、管理后台四个主要页面添加加载骨架屏（Skeleton Screen）。页面打开时先展示灰色占位块（模拟内容区域的形状），100-300ms后淡出并显示实际内容。骨架屏形状需匹配各页面实际布局（如首页：搜索框占位+分类网格占位+热门标签占位）。 |
| **变更理由** | 骨架屏是提升感知性能的标准做法；让页面加载过渡更自然；在原型演示时展示完整的前端体验考量。 |
| **受影响文件** | `index.html`、`forum.html`、`personal.html`、`admin.html`（骨架屏DOM）、`styles.css`（骨架屏样式+闪烁动画）、`script.js`（骨架屏显示/隐藏控制） |
| **优先级** | 中 |
| **工作量** | 中（约需1天） |

---

### CR-039 深色模式CSS变量化（从手动覆盖改为CSS变量体系） F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | 设计系统文档已定义完整的深色/浅色色彩系统，但CSS实现中深色模式通过大量手动覆盖样式实现 |
| **变更描述** | 将整个样式系统重构为CSS自定义属性（CSS Variables）体系。在`:root`中定义所有设计Token（--color-primary, --color-bg, --color-text-primary等），`.dark-mode`下切换变量值。各组件样式通过`var(--xxx)`引用变量。重构后深色模式的CSS代码量预计减少60%以上。 |
| **变更理由** | 当前深色模式通过大量`.dark-mode .xxx`覆盖实现，维护成本高且容易遗漏；CSS变量是现代前端样式管理的最佳实践；便于后续新增主题色。 |
| **受影响文件** | `styles.css`（重构约577行）、全部HTML页面的内联style需同步改为变量引用 |
| **优先级** | 高 |
| **工作量** | 大（约需1-2天） |

---

### CR-040 全站增加键盘导航支持 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | UML概述演讲中讨论的"系统质量属性"提及易用性 |
| **变更描述** | 为全站增加键盘导航支持：①Tab键可在所有可交互元素（按钮/输入框/链接/选择器）间切换，焦点样式（蓝色outline）清晰可见；②Enter/Space可激活当前焦点元素；③Esc可关闭模态弹窗/下拉菜单/Toast消息；④在问答页，Ctrl+Enter可发送消息。焦点顺序遵循视觉布局从左到右、从上到下。 |
| **变更理由** | 键盘导航是无障碍访问的基础要求；部分用户（如行政人员）习惯键盘操作；提升原型在评审中的"专业度"印象。 |
| **受影响文件** | 全部7个HTML页面（确保tabindex合理）、`script.js`（全局键盘事件监听）、`styles.css`（焦点样式） |
| **优先级** | 中 |
| **工作量** | 中（约需1天） |

---

### CR-041 Toast消息组件统一化 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | 当前各页面Toast消息样式不一致——有的用浏览器alert（不推荐），有的用自定义div |
| **变更描述** | 创建统一的Toast消息组件，支持四种类型：成功（绿色）、错误（红色）、警告（橙色）、信息（蓝色）。所有页面的操作反馈统一使用该组件。Toast从页面顶部居中滑入，3秒后自动滑出消失。支持手动点击关闭。同类型Toast不重复堆叠（后续消息替换前一条同类型消息）。 |
| **变更理由** | 当前存在alert和自定义Toast混用的情况，体验不一致；统一Toast组件减少重复代码；滑入/滑出动画提升品质感。 |
| **受影响文件** | `script.js`（新增createToast函数，替换全部alert调用）、全部HTML页面（引用统一Toast）、`styles.css`（Toast动画） |
| **优先级** | 中 |
| **工作量** | 中（约需半天） |

---

### CR-042 表单提交按钮增加loading状态 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | 设计系统文档中定义了"加载：旋转动画"交互状态，但实际未应用 |
| **变更描述** | 所有表单提交按钮（登录、注册、发帖、评论、反馈提交等）在点击后进入loading状态：按钮文字变为"提交中..."，左侧显示旋转加载图标，按钮disabled防止重复提交。1-2秒后（模拟网络延迟）恢复或跳转。 |
| **变更理由** | 防止用户重复点击提交；加载状态是表单交互的完整闭环；设计文档中已定义但实际未实现。 |
| **受影响文件** | 所有含表单的HTML页面、`script.js`（showLoading/hideLoading工具函数）、`styles.css`（按钮loading样式） |
| **优先级** | 中 |
| **工作量** | 小（约需2-3小时） |

---

### CR-043 页面标题（document.title）动态化 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | 当前所有页面title格式不统一（有的带"校务问答机器人系统"后缀，有的没有） |
| **变更描述** | 统一页面标题格式为"页面名 - 校务问答机器人"。当有新通知未读时，标题前缀增加"(N)"——如"(3)问答首页 - 校务问答机器人"，模拟新消息提醒效果。用户切换到其他Tab时标题闪烁以吸引注意（用setInterval切换"(3) "和原始标题）。 |
| **变更理由** | 统一标题提升浏览器标签页的识别性；未读计数是Web应用中吸引用户回看的经典技巧。 |
| **受影响文件** | 全部7个HTML页面（title标签统一）、`script.js`（标题动态更新逻辑） |
| **优先级** | 低 |
| **工作量** | 小（约需1-2小时） |

---

### CR-044 全站图片alt文本规范化 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | 设计系统文档未涉及可访问性规范 |
| **变更描述** | 检查全部7个HTML页面，为所有img标签和Icon（如Font Awesome图标在屏幕阅读器场景下）补充有意义的文本替代：①功能性图标（如搜索按钮图标）添加aria-label属性；②纯装饰性图标添加aria-hidden="true"；③分类卡片、热门问题等图标确保其语义可被屏幕阅读器获取。 |
| **变更理由** | 可访问性在课程评审中可能是加分项；仅涉及HTML属性修改，成本低、无副作用。 |
| **受影响文件** | 全部7个HTML页面（alt/aria-label/aria-hidden属性补充） |
| **优先级** | 低 |
| **工作量** | 小（约需1-2小时） |

---

### CR-045 CSS动画增加prefers-reduced-motion媒体查询适配 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟡 非功能需求变更 |
| **变更来源** | 设计系统文档未提及"减少动画"偏好适配 |
| **变更描述** | 在所有CSS动画和过渡效果处增加`@media (prefers-reduced-motion: reduce)`查询：当用户操作系统开启了"减少动画"（如Windows的"显示动画"关闭），页面禁用所有非必要的过渡动画（淡入淡出、滑动、旋转等），仅保留必要的功能动画（如loading旋转）。 |
| **变更理由** | 部分用户对动画敏感（晕动症）；`prefers-reduced-motion`是Web标准媒体查询；实现简单（几行CSS），展示无障碍意识。 |
| **受影响文件** | `styles.css`（增加@media查询包裹动画样式） |
| **优先级** | 低 |
| **工作量** | 小（约需1-2小时） |

---

## 五、业务规则变更类（5条）

### CR-046 问题分类的优先级权重调整 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟠 业务规则变更 |
| **变更来源** | QFD打分表格中未体现分类维度的权重差异 |
| **变更描述** | 为8个问题分类设置不同的展示权重：①学业相关（权重5——最高频）；②宿舍生活（权重4）；③校园卡（权重4）；④奖学金（权重3）；⑤考试报名（权重3）；⑥图书馆（权重3）；⑦请假申请（权重2）；⑧其他（权重1）。在热门问题和分类排序中，高权重分类的问题优先展示。权重值在前端以data-weight属性维护。 |
| **变更理由** | 不同分类的实际使用频率确实存在差异；通过权重可让首页展示更贴合真实使用场景。 |
| **受影响文件** | `script.js`（分类权重配置+排序逻辑） |
| **优先级** | 低 |
| **工作量** | 小（约需1小时） |

---

### CR-047 匿名用户的功能权限限制规则 T

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟠 业务规则变更 |
| **变更来源** | NFR-027（原型无真实后端身份核验）——未登录状态下的权限未明确定义 |
| **变更描述** | 定义未登录用户（访客）的功能权限边界：✅允许——浏览问答首页、使用搜索提问、查看热门问题、查看帮助页、查看校园黄页；❌不允许——查看历史对话、点赞/收藏、发表评论、发布帖子、使用个人中心、访问管理后台。当访客尝试受限操作时，弹出提示"请先登录"，并提供"去登录"按钮。 |
| **变更理由** | 当前原型中未登录状态的行为不明确；明确的权限边界是权限管理的基础；"请先登录"引导增加了登录转化率。 |
| **受影响文件** | `script.js`（全局权限检查函数）、涉及操作的各HTML页面（调用权限检查） |
| **优先级** | 中 |
| **工作量** | 小（约需2-3小时） |

---

### CR-048 论坛发帖内容最小长度和反垃圾规则 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟠 业务规则变更 |
| **变更来源** | AD-022（设置发帖审核规则）——当前为mock占位 |
| **变更描述** | 定义发帖和评论的内容规则（前端模拟校验）：①标题至少5个字；②正文至少10个字；③不允许纯数字/纯符号/纯空格的内容；④同一用户60秒内最多发表1篇帖子（防止刷屏，模拟限流）；⑤评论至少2个字。违反规则时给出具体提示（而非笼统的"内容不符合要求"）。 |
| **变更理由** | 内容规则是论坛运营的基础；具体的前端校验可在原型中完整演示"违规-提示-修正"的交互闭环。 |
| **受影响文件** | `forum.html`（发帖表单验证）、`post.html`（评论验证）、`script.js`（规则引擎+限流计时器） |
| **优先级** | 中 |
| **工作量** | 小（约需2-3小时） |

---

### CR-049 问答机器人"置信度"显示规则 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟠 业务规则变更 |
| **变更来源** | 用例文档中"无法回答兜底指引"仅区分了"能回答"和"不能回答"二元情况 |
| **变更描述** | 在mock答案数据中为每条回答增加"置信度"字段（高/中/低三档），并在答案展示区以不同方式呈现：①高置信度（≥80%匹配）：直接展示答案，无额外提示；②中置信度（50-79%）：答案展示后附加"💡 以上信息仅供参考，建议向相关部门确认"；③低置信度（<50%）或无匹配：展示兜底指引（电话+部门+办公地点）。同时在答案末尾以灰色小字标注"匹配度：XX%"。 |
| **变更理由** | 二元（能答/不能答）过于粗糙；置信度分级可展示更真实的问答系统行为；让原型在评审中体现更细致的需求思考。 |
| **受影响文件** | `script.js`（mock数据增加confidence字段+置信度渲染逻辑）、`index.html`（置信度提示DOM） |
| **优先级** | 中 |
| **工作量** | 小（约需2-3小时） |

---

### CR-050 个人数据"导出与清空"规则 F

| 字段 | 内容 |
|------|------|
| **变更类型** | 🟠 业务规则变更 |
| **变更来源** | 对象图中明确指出所有数据为"黄色=会话级"或"蓝色=硬编码"，用户无法管理自己的数据 |
| **变更描述** | 在个人中心设置页增加"数据管理"区域，提供两个操作：①导出我的数据——将当前用户在本地的聊天记录、收藏、发帖记录打包为一个JSON文件下载；②清空我的数据——清除该用户在本地的全部数据（聊天记录、收藏、草稿），操作前弹出二次确认"确定要清空吗？此操作不可撤销"。清空后用户数据恢复初始状态，硬编码的mock基础数据不受影响。 |
| **变更理由** | 赋权用户管理自己的数据是基本的产品伦理；导出和清空在当前纯前端架构下完全可实施（localStorage + JSON下载）；体现数据管理闭环。 |
| **受影响文件** | `personal.html`（数据管理区域）、`script.js`（导出JSON+清空localStorage逻辑） |
| **优先级** | 中 |
| **工作量** | 小（约需2-3小时） |

---

## 附录A：变更统计汇总

| 变更类型 | 数量 | 编号 |
|----------|------|------|
| 🟢 功能新增 | 16 | CR-001 ~ CR-016 |
| 🔵 功能修改 | 16 | CR-017 ~ CR-032 |
| 🔴 功能删除 | 5 | CR-033 ~ CR-037 |
| 🟡 非功能需求变更 | 8 | CR-038 ~ CR-045 |
| 🟠 业务规则变更 | 5 | CR-046 ~ CR-050 |

| 优先级 | 数量 | 占比 |
|--------|------|------|
| 高 | 9 | 18% |
| 中 | 25 | 50% |
| 低 | 16 | 32% |

| 受影响文件 | 涉及变更数 |
|------------|-----------|
| `script.js` | 48 |
| `styles.css` | 25 |
| `index.html` | 19 |
| `personal.html` | 14 |
| `admin.html` | 11 |
| `forum.html` | 9 |
| `post.html` | 7 |
| `login.html` | 4 |
| `help.html` | 3 |
| 新建页面（notifications / directory） | 2 |

| 变更来源（主要依据） | 涉及变更数 |
|---------------------|-----------|
| 教师访谈反馈 | 12 |
| QFD需求矩阵 | 11 |
| 原型内部评审/测试意见 | 10 |
| 设计系统/协作图/对象图等建模文档 | 9 |
| 项目章程/可行性报告等管理文档 | 5 |
| 用户手册/测试用例 | 3 |

---

## 附录B：各变更与现有需求/文档的追溯关系

| 变更ID | 关联的现有需求ID | 关联的文档 |
|--------|-----------------|-----------|
| CR-001 | UR-015 (兜底指引) | 骆彦余辅导员访谈 |
| CR-002 | — | 苏奎老师访谈 |
| CR-003 | UR-009 (点赞/收藏) | QFD需求矩阵 |
| CR-004 | — | 测试用例文档 |
| CR-005 | AD-002~005 (统计) | QFD需求矩阵 |
| CR-006 | — | 原型评审记录 |
| CR-007 | UR-032 (深浅色主题) | 设计系统文档 |
| CR-008 | AD-019 (发布公告) | QFD需求矩阵 |
| CR-009 | — | 需求获取记录 |
| CR-010 | UR-001/002 (注册/登录) | 测试用例文档 |
| CR-011 | UR-015 (兜底指引) | QFD需求矩阵 |
| CR-012 | — | 王豪烨老师访谈 |
| CR-013 | — | 协作图 |
| CR-014 | — | 用户手册 |
| CR-015 | UR-005 (分类导航) | 原型评审记录 |
| CR-016 | UR-014 (导出历史对话) | QFD需求矩阵 |
| CR-017 | NFR-015 (问答结构化呈现) | 设计系统文档 |
| CR-018 | UR-022 (按分类筛选) | 教师访谈汇总 |
| CR-019 | AD-006 (查询知识库) | QFD需求矩阵 |
| CR-020 | — | 原型评审记录 |
| CR-021 | — | 设计系统文档 |
| CR-022 | UR-025 (发布新帖子) | 协作图 |
| CR-023 | UR-029 (热门话题榜单) | QFD需求矩阵 |
| CR-024 | UR-030 (校务消息推送) | QFD需求矩阵 |
| CR-025 | UR-001/002 (注册/登录) | 测试用例文档 |
| CR-026 | — | 苏奎老师访谈 |
| CR-027 | — | 设计系统文档 |
| CR-028 | AD-010~012 (用户管理) | QFD需求矩阵 |
| CR-029 | — | 设计系统文档 |
| CR-030 | UR-033 (查看帮助) | QFD需求矩阵 |
| CR-031 | UR-009 (点赞/收藏) | QFD需求矩阵 |
| CR-032 | AD-016~018 (反馈管理) | QFD需求矩阵 |
| CR-033 | UR-019 (AI模型切换) | 可行性研究报告 |
| CR-034 | — | 王硕苹老师访谈 |
| CR-035 | — | 设计系统文档 |
| CR-036 | — | FINAL_CHECKLIST |
| CR-037 | UR-025 (发布新帖子) | 项目完成总结 |
| CR-038 | NFR-006 (无停顿感) | QFD需求矩阵 |
| CR-039 | NFR-009~011 (深色模式) | 设计系统文档 |
| CR-040 | NFR-012~014 (易用性) | UML演讲 |
| CR-041 | — | FINAL_CHECKLIST |
| CR-042 | — | 设计系统文档 |
| CR-043 | — | — |
| CR-044 | — | 设计系统文档 |
| CR-045 | — | — |
| CR-046 | UR-005 (分类导航) | QFD打分表格 |
| CR-047 | NFR-027 (无真实鉴权) | QFD需求矩阵 |
| CR-048 | AD-022 (发帖审核规则) | QFD需求矩阵 |
| CR-049 | UR-015 (兜底指引) | 用例文档 |
| CR-050 | NFR-024~028 (安全隐私) | 对象图 |

---

> **版本**：v2.0（原型适用版）  
> **生成日期**：2026年6月7日  
> **适用范围**：仅限前端原型（HTML/CSS/JS + mock数据），所有变更均可在无后端环境下模拟实现并演示。  
> **总变更数**：50条
